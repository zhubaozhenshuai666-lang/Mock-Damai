@baseUrl = http://localhost:8081
@phone = 13900000001
@password = Test123456
@bearerToken = paste-login-token-here

### 使用 @phone 前，先按 phase1-auth-api.http 中的 /api/auth/register 注册该用户；或改为本地已有账号。
### 当前用户资料只提供 /api/users/me；按 ID 查询和 /api/users 创建接口已移除。

### 1. 登录，复制 data.token 到 @bearerToken
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "phone": "{{phone}}",
  "password": "{{password}}"
}

### 2. 查询当前登录用户资料
GET {{baseUrl}}/api/users/me
Authorization: Bearer {{bearerToken}}

### 3. 未带 token 查询用户资料，应被拒绝
GET {{baseUrl}}/api/users/me
