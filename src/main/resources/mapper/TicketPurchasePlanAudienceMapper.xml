<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zewbby.smartticket.mapper.TicketPurchasePlanAudienceMapper">

    <resultMap id="TicketPurchasePlanAudienceResultMap"
               type="com.zewbby.smartticket.domain.entity.TicketPurchasePlanAudience">
        <id property="id" column="id"/>
        <result property="planId" column="plan_id"/>
        <result property="audienceId" column="audience_id"/>
        <result property="selectionType" column="selection_type"/>
        <result property="lineNo" column="line_no"/>
        <result property="nameSnapshot" column="name_snapshot"/>
        <result property="idNoHashSnapshot" column="id_no_hash_snapshot"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertBatch">
        INSERT INTO ticket_purchase_plan_audience (
            plan_id,
            audience_id,
            selection_type,
            line_no,
            name_snapshot,
            id_no_hash_snapshot,
            created_at
        ) VALUES
        <foreach collection="audiences" item="item" separator=",">
            (
                #{item.planId},
                #{item.audienceId},
                #{item.selectionType},
                #{item.lineNo},
                #{item.nameSnapshot},
                #{item.idNoHashSnapshot},
                #{item.createdAt}
            )
        </foreach>
    </insert>

    <delete id="deleteByPlanIdAndSelectionType">
        DELETE FROM ticket_purchase_plan_audience
        WHERE plan_id = #{planId}
          AND selection_type = #{selectionType}
    </delete>

    <select id="selectByPlanIdAndSelectionType" resultMap="TicketPurchasePlanAudienceResultMap">
        SELECT
            id,
            plan_id,
            audience_id,
            selection_type,
            line_no,
            name_snapshot,
            id_no_hash_snapshot,
            created_at
        FROM ticket_purchase_plan_audience
        WHERE plan_id = #{planId}
          AND selection_type = #{selectionType}
        ORDER BY line_no ASC
    </select>
</mapper>
